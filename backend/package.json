{
  "scripts": {
    "dev": "PORT=4000 nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "build": "tsc -p . --outDir build/",
    "start": "PORT=80 node build/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "resend": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1"
  }
}
