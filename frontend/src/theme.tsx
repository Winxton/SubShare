// theme.ts
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      500: "#ff5349", // Primary color
    },
    secondary: {
      600: "#D3D3D3", //secondary color
    },
  },
});

export default theme;
